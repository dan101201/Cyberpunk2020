<Page x:Class="NetCore3Cyberpunk.Characters"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NetCore3Cyberpunk.Backend"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Characters">

    <Grid>
        <Grid Margin="0,0,557,0">
            <Grid Margin="0,0,0,422">
                <Button Click="AddNewCharacter" Margin="209,0,0,0" Foreground="{x:Null}" BorderBrush="{x:Null}" Background="{x:Null}">
                    <Image Source="plus.png"/>
                </Button>
            </Grid>
            <ScrollViewer Margin="0,33,0,0">
                
            </ScrollViewer>
        </Grid>
        
        <Frame Name="CharacterDetails" Margin="248,0,0,0">
            
        </Frame>
    </Grid>
</Page>
